name: build_runner_test
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  path: ^1.9.0
  build: ^4.0.4

dev_dependencies:
  build_runner: ^2.12.2
  lints: ^6.0.0
  test: ^1.25.6
